<!--completed = Grocery_ListItem.objects.filter(is_complete=True)
incomplete = Grocery_ListItem.objects.filter(is_complete=False)
{% block content %}
  <h1>Completed List</h1>
  {% if  %}
  <ul>
    {% i in book_list %}
      <li>
        <a href="{{ book.get_absolute_url }}">{{ book.title }}</a> ({{book.author}})
      </li>
    {% endfor %}
  </ul>
  {% else %}
    <p>There are no books in the library.</p>
  {% endif %}
{% endblock %} -->

<!--<form action="/completeGrocery_ListItem/" method = "post">
  {% csrf_token %}
  <input type="checkbox" id="is_complete" name="is_complete" value="0"
  <label for="is_complete">Check When Completed</label> 
  <button type="submit">Submit</button>
</form> -->

# def completeGrocery_ListView(request):
#     x = request.POST['is_complete']
#     complete_item = Grocery_ListItem(is_complete = x)
#     complete_item.save()
#     return HttpResponseRedirect('/grocery_list_app/') 